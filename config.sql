-- Configure mode and headers output
-- Note: only needs to be configured once
-- One file is for one purpose - that's why everything is broken out in the SQL files
.mode columns
-- run `.help .mode` to view other options

.headers on
